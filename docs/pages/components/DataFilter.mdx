---
title: DataFilter
---

import CodeExample from 'nextra-theme-mui-docs/CodeExample';

# DataFilter

A rich interface to create data filters. Filters are represented as a set of operations on individual fields of the data records. Each operation is represented by a `Chip`.

## Basic DataFilter

import Basic from '../../examples/DataFilter/Basic';
import srcBasic from '../../examples/DataFilter/Basic?raw';

<CodeExample src={srcBasic}>
  <Basic />
</CodeExample>

## Custom Operators

You can define your own operators.

import CustomOperator from '../../examples/DataFilter/CustomOperator';
import srcCustomOperator from '../../examples/DataFilter/CustomOperator?raw';

<CodeExample src={srcCustomOperator}>
  <CustomOperator />
</CodeExample>

## Different Sizes

The `DataFilter` component supports different sizes through the `size` property.

import DifferentSizes from '../../examples/DataFilter/DifferentSizes';
import srcDifferentSizes from '../../examples/DataFilter/DifferentSizes?raw';

<CodeExample src={srcDifferentSizes}>
  <DifferentSizes />
</CodeExample>
