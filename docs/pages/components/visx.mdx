import CodeExample from 'nextra-theme-mui-docs/CodeExample';

# visx

Theme support and helper components to build `visx/xychart` charts that adhere to Material design styleguide.

## Bars

Recreates a bar chart example from Material Design docs. It showcases how the theme can be set up.

import BasicBars from '../../examples/visx/BasicBars';
import srcBasicBars from '../../examples/visx/BasicBars?raw';

<CodeExample src={srcBasicBars}>
  <BasicBars />
</CodeExample>

## Glyphs

Recreates a chart example from Material Design docs with custom glyphs.

import BasicGlyphs from '../../examples/visx/BasicGlyphs';
import srcBasicGlyphs from '../../examples/visx/BasicGlyphs?raw';

<CodeExample src={srcBasicGlyphs}>
  <BasicGlyphs />
</CodeExample>

## Lines

Recreates a temperature chart example from Material Design docs. It showcases how to set up a Material Design styled tooltip.

import Temperatures from '../../examples/visx/Temperatures';
import srcTemperatures from '../../examples/visx/Temperatures?raw';

<CodeExample src={srcTemperatures}>
  <Temperatures />
</CodeExample>
